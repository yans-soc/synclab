services:
  postgres:
    image: postgres:16-alpine
    container_name: synclab-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-synclab}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-synclab}
      POSTGRES_DB: ${POSTGRES_DB:-synclab}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
